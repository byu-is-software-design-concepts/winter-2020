export const scramble = (str1, str2) => {
  var count
  let txtmatch = true
  if(txtmatch)
    while(count < str2.length) {
     {
      if(str1.includes(str2[count])) {
        txtmatch = true
      }
      else {
        contains = false
        txtmatch = str2.length
      }
      
    }
  }
  return txtmatch
}
